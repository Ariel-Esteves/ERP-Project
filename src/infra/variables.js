const base_url = "http://localhost:8080";

const auth_url = base_url + "/auth";

const cep_url = base_url + "/cep";

const person_url = base_url + "/person";

const product_url = base_url + "/product";

const sale_url = base_url + "/sale";

const stock_url = base_url + "/stock";

const user_url = base_url + "/user";

const wallet_url = base_url + "/wallet";

const expiration_time = 1000 * 60 * 120;

export {
  auth_url,
  cep_url,
  person_url,
  product_url,
  sale_url,
  stock_url,
  user_url,
  wallet_url,
  expiration_time,
};
